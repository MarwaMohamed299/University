@model UniversitySystem.Models.Tma

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Tma</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CourseId" class="control-label"></label>
                <input asp-for="CourseId" class="form-control" />
                <span asp-validation-for="CourseId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tmaid" class="control-label"></label>
                <input asp-for="Tmaid" class="form-control" />
                <span asp-validation-for="Tmaid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tmaletter" class="control-label"></label>
                <input asp-for="Tmaletter" class="form-control" />
                <span asp-validation-for="Tmaletter" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
